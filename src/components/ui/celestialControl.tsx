import Celestial from "../../models/celestial";

interface Props{
    data:  Celestial;
    update: Function;
}

export default function CelestialControl({data, update} : Props){

    return (<>
    
    </>)
}